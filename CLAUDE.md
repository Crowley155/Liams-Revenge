# USDWatch Claude Instructions

Before changing UI, read `.impeccable.md` and, when present, `C:\Users\willi\.codex\design.md`.

Apply the global UI guardrails automatically:

- Build actual app workflows before marketing surfaces.
- Keep parent-facing screens calm, practical, and readable.
- Use existing components, tokens, and layout patterns first.
- Avoid AI/SaaS visual tells: gradient text, decorative blur/orb backgrounds, glassmorphism, cards inside cards, repeated feature-card grids, fake metrics, and generic dark neon tech styling.
- Make empty, loading, error, disabled, hover, focus, active, selected, long-text, mobile, and narrow-container states explicit.
- Use familiar icons for familiar actions.
- Keep copy direct and useful.
- Verify responsive behavior and text fit before calling UI complete.

Do not edit `deliverables/` or `superpowers/` unless explicitly asked.
